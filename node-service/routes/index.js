const test = require('./test')
const serviceSpace = require('./service.space')
const service = require('./service')
const localFile = require('./local.file')
module.exports = {
  test, serviceSpace, service, localFile
};
